package com.example.challengeAlkemy.enums;

public enum RolNombre {

	//El aplicativo tiene dos roles:
    // admin(Tiene todos los permisos CRUD) y user(solo consultas)
	
    ROLE_ADMIN, ROLE_USER;
}
